# Base de Datos
# Para desarrollo local con SQLite (opcional, comentado por defecto):
# DATABASE_URL=file:./prisma/dev.db

# Para producciÃ³n en Vercel con PostgreSQL (requerido):
# ObtÃ©n esta URL desde tu proveedor de base de datos PostgreSQL (Vercel Postgres, Neon, Supabase, etc.)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname?schema=public

# Entorno
# Next.js detecta automÃ¡ticamente NODE_ENV, pero puedes configurarlo explÃ­citamente
# NODE_ENV=production

# Notas:
# - En Vercel, configura DATABASE_URL en el panel de Variables de Entorno
# - No comitees este archivo con valores reales
# - Para desarrollo local, puedes usar SQLite temporalmente cambiando el provider en schema.prisma
